{
  "name": "markdown-editor-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
